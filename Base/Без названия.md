FROM dockerhub.mos.ru/maven:3.9.9-eclipse-temurin-17 as runtime
ENV TZ=Europe/Moscow
RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && \
    echo $TZ > /etc/timezone
# RUN adduser --system --group --home /opt/app appuser
# USER appuser
WORKDIR /opt/app
ENTRYPOINT ["java", "-Dio.netty.transport.native.epoll=false", "-Djava.net.preferIPv4Stack=true", "-jar", "/opt/app/application.jar"]
HEALTHCHECK --start-period=30s --interval=30s --timeout=3s --retries=3 \
            CMD curl --silent --fail --request GET http://localhost:8287/actuator/health \
                | jq --exit-status '.status == "UP"' || exit 1
#-------------------------------------------------------------------------------
FROM runtime AS gateway-kafka-temporal
COPY target/gateway-kafka-temporal*.jar application.jar