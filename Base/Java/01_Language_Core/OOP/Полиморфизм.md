# Полиморфизм в Java

> **Полиморфизм** — один интерфейс, разные реализации. **Статический** (overloading) разрешается на этапе компиляции по типу ссылки. **Динамический** (overriding) разрешается в runtime через vtable (`invokevirtual`). Тип объекта, а не тип ссылки, определяет какой метод вызвать.
> На интервью: vtable structure (O(1) lookup), inline caching (mono→bi→mega), `final`/`private` devirtualization, overloading по типу ссылки (ловушка), `sealed` + JIT.

## Связанные темы

[[Наследование]], [[Интерфейсы]], [[Приведение типов, widening conversion, narrowing conversion]], [[JIT Compiler & Optimizations]]

---

## Статический vs Динамический полиморфизм

```java
// Статический (overloading) — компилятор выбирает по типу ССЫЛКИ:
class Printer {
    void print(Object o)  { System.out.println("Object"); }
    void print(String s)  { System.out.println("String"); }
}
Object obj = "hello";          // тип ссылки: Object
new Printer().print(obj);      // → "Object" ! (не "String")
// Компилятор видит Object → выбирает print(Object) статически

// Динамический (overriding) — JVM выбирает по типу ОБЪЕКТА:
Animal a = new Dog();
a.makeSound();  // → Dog.makeSound() — runtime dispatch через vtable
```

**Ловушка:** overloading — это **не** полиморфизм. Это несколько методов с одним именем. Путаница — типичная ошибка на Senior интервью.

---

## Принцип подстановки Лисков (LSP)

```java
// LSP: подкласс должен быть полностью заменим родителем — без сюрпризов

// Нарушение LSP:
class Rectangle {
    protected int w, h;
    void setWidth(int w)  { this.w = w; }
    void setHeight(int h) { this.h = h; }
    int area() { return w * h; }
}
class Square extends Rectangle {
    @Override void setWidth(int w)  { this.w = this.h = w; } // НАРУШЕНИЕ
    @Override void setHeight(int h) { this.w = this.h = h; } // LSP — неожиданное поведение
}
// Код, работающий с Rectangle, получит неожиданный результат с Square

// Решение: использовать sealed + records + интерфейс
sealed interface Shape permits Circle, Rect {}
record Circle(double r) implements Shape {}
record Rect(double w, double h) implements Shape {}
```

---

## Abstract class / Interface polymorphism

```java
// Интерфейс — максимальная гибкость:
interface Validator<T> {
    boolean validate(T value);
    default Validator<T> and(Validator<T> other) {
        return v -> this.validate(v) && other.validate(v);
    }
}

Validator<String> notEmpty = s -> !s.isBlank();
Validator<String> maxLen   = s -> s.length() <= 100;
Validator<String> combined = notEmpty.and(maxLen);
combined.validate("hello"); // true
```

---

## Вопросы на интервью

- Как работает vtable? Какова сложность `invokevirtual`?
- Что такое inline caching? Mono → bi → megamorphic — как влияет на производительность?
- Почему `final`/`private` методы быстрее виртуальных?
- Overloading разрешается в compile time или runtime?
- Что такое LSP? Как его нарушение проявляется на практике?
- Как `sealed` классы помогают JIT оптимизировать полиморфные вызовы?

---

## Подводные камни

- **Overloading по типу ссылки** — `print(Object)` вызовется для `Object obj = "str"`, даже если есть `print(String)`. Overloading — compile-time, смотрит на тип ссылки.
- **Default method conflict** — класс `C implements A, B` где оба имеют `default void method()` → ошибка компиляции. Надо явно: `A.super.method()` или override.
- **Мегаморфный call site** — если в цикле `shape.area()` приходят 5+ разных типов Shape → JIT прекращает инлайнинг → падение производительности. Однотипные списки дают монофонический dispatch и инлайнинг.

---

## Senior Insights

### S1. vtable: как JVM разрешает виртуальный вызов

Каждый класс в Metaspace имеет **vtable** — массив указателей на методы. При наследовании подкласс копирует vtable родителя и заменяет переопределённые слоты.

```
Object vtable:     [0: equals, 1: hashCode, 2: toString, ...]
Animal vtable:     [0: equals, 1: hashCode, 2: toString, 3: makeSound(Animal.makeSound), ...]
Dog vtable:        [0: equals, 1: hashCode, 2: toString, 3: makeSound(Dog.makeSound), ...]
                                                                         ↑
                                                         Dog заменил слот 3
```

При `invokevirtual`:
```
1. Взять ссылку на объект (Dog)
2. Загрузить klass pointer из object header
3. Найти vtable[slot_3] → Dog.makeSound
4. Вызвать
```
Всё — O(1). Индекс слота статически вычислен компилятором.

### S2. Inline Cache: монофонический → биморфный → мегаморфный

JIT не просто вызывает vtable — он кэширует результат:

```
Monomorphic call site (1 тип):
  [type_guard: if obj.klass == Dog] → Dog.makeSound() ← INLINED
  JIT убирает vtable lookup + может инлайнить тело метода

Bimorphic (2 типа):
  if obj.klass == Dog  → Dog.makeSound()  ← INLINED
  if obj.klass == Cat  → Cat.makeSound()  ← INLINED
  else → slow path (vtable dispatch)

Megamorphic (3+ типов):
  vtable dispatch — инлайнинг ОТКЛЮЧЁН
  Нет guard → нет inlining → нет constant folding и других оптимизаций
```

**Реальный пример мегаморфного call site:**
```java
// BAD: shapes содержит Circle, Square, Triangle, Pentagon...
for (Shape shape : shapes) {
    total += shape.area(); // мегаморфный — JIT не инлайнит
}

// GOOD: однотипный список → монофонический → JIT инлайнит
List<Circle> circles = ...;
for (Circle c : circles) {
    total += c.area(); // JIT: "всегда Circle" → инлайнинг!
}
```

**Диагностика:** `-XX:+PrintInlining` — смотри `not inlined, megamorphic`.

### S3. final и private → devirtualization без guard

```java
// final метод → JIT использует invokespecial (не invokevirtual)
// → гарантированный инлайнинг без deoptimization guard
class Calculator {
    final double compute(double x) { return x * x; }
}

// private → тоже invokespecial
class Service {
    private void validate(Request r) { ... }
    public void process(Request r) {
        validate(r); // invokespecial → JIT инлайнит без guard
    }
}
```

**Почему это важно:** при `invokevirtual` JIT добавляет guard: `if (type != expected) deoptimize`. С `final`/`private` guard не нужен → нет риска деоптимизации.

### S4. Перегрузка (overloading) разрешается статически — ловушка

```java
class Printer {
    void print(Object o)  { System.out.println("Object"); }
    void print(String s)  { System.out.println("String"); }
}

Object obj = "hello";      // тип ссылки: Object
Printer p = new Printer();
p.print(obj);              // → "Object" !
// Компилятор выбрал перегрузку по ТИПУ ССЫЛКИ (Object), не по факту (String)
// Это НЕ полиморфизм — это статический выбор

String str = "hello";
p.print(str);              // → "String"
```

> [!WARNING] Перегрузка ≠ полиморфизм
> Переопределение (overriding) разрешается в runtime по факту объекта — это настоящий полиморфизм. Перегрузка (overloading) разрешается в **compile time** по типу ссылки — это просто несколько методов с одним именем. Путаница здесь — типичная ошибка на Senior интервью.

### S5. sealed + switch → JIT perfect optimization

```java
sealed interface Expr permits Num, Add, Mul {}
record Num(int v) implements Expr {}
record Add(Expr l, Expr r) implements Expr {}
record Mul(Expr l, Expr r) implements Expr {}

// JIT видит закрытое множество типов:
int eval(Expr e) {
    return switch (e) {
        case Num(int v)          -> v;
        case Add(var l, var r)   -> eval(l) + eval(r);
        case Mul(var l, var r)   -> eval(l) * eval(r);
    };
}
// Компилятор проверяет exhaustiveness
// JIT девиртуализирует все ветки — нет мегаморфного dispatch
```

> [!INFO] Связано
> - [[Интерфейсы]] — invokeinterface vs invokevirtual, inline cache детали
> - [[Наследование]] — invokespecial, bridge methods, конструкторы
> - [[JIT Compiler & Optimizations]] — деоптимизация, inlining, монофонический dispatch
