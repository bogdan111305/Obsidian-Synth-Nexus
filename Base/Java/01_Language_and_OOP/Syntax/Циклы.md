---
title: "Java — Циклы"
tags: [java, loops, control-flow, basics]
updated: 2026-03-04
---

# Циклы в Java

> [!QUOTE] Суть
> **Циклы** выполняют повторяющиеся действия, пока условие истинно. В Java: `while`, `do-while`, `for`, `for-each` + функциональный стиль через Stream API. Выбор цикла влияет на читаемость и производительность.
## Цикл `while` и `do-while`

### Цикл `while`

- Выполняет блок кода, пока условие истинно (`true`).
- Подходит для случаев, когда количество итераций заранее неизвестно.

```java
int i = 0;
while (i < 5) {
    System.out.println("i = " + i);
    i++;
}
```
### Цикл `do-while`

- Условие проверяется **после** итерации, тело выполнится хотя бы один раз.

```java
int i = 0;
do {
    System.out.println("i = " + i);
    i++;
} while (i < 5);
```

> Используйте `while` для неизвестного числа итераций, `do-while` — если нужно выполнить хотя бы раз.
## Цикл `for` и `for-each`

### Цикл `for`

- Удобен, когда количество итераций известно или выражается переменной.

```java
for (int i = 0; i < 5; i++) {
    System.out.println("i = " + i);
}
```

- Можно использовать обратный цикл, несколько переменных, цикл без тела, бесконечный цикл (`for (;;) { ... }`).
### Цикл `for-each` (Java 5+)

- Предназначен для перебора коллекций или массивов.

```java
List<String> list = List.of("A", "B", "C");
for (String s : list) {
    System.out.println(s);
}
```

> Используйте `for-each` для коллекций и массивов, если не нужен индекс.
## Stream API: `stream().forEach()`

- Позволяет выполнять действия для каждого элемента коллекции в функциональном стиле.
- Удобен для сложных цепочек операций (`filter`, `map`, ...), поддерживает параллельную обработку.

```java
List<String> list = List.of("A", "B", "C");
list.stream().forEach(s -> System.out.println("Элемент: " + s));
```

- Для параллельной обработки используйте `parallelStream().forEach()`, но порядок не гарантирован.
- Для упорядоченной обработки используйте `forEachOrdered()`.

> **Совет:** Для простых операций на небольших коллекциях обычные циклы быстрее. Stream API удобен для сложных преобразований и больших данных.
## Рекурсия

- Метод вызывает сам себя для решения подзадачи.
- Подходит для задач с естественной рекурсивной структурой (обход деревьев, факториал, Фибоначчи).

```java
public static long factorial(int n) {
    if (n == 0 || n == 1) return 1;
    return n * factorial(n - 1);
}
```
### Мемоизация

> **Мемоизация** — это техника кэширования результатов рекурсивных вызовов для ускорения вычислений и избежания повторных операций.

```java
import java.util.HashMap;
import java.util.Map;

Map<Integer, Long> memo = new HashMap<>();
long fib(int n) {
    if (n <= 1) return n;
    if (memo.containsKey(n)) return memo.get(n);
    long result = fib(n - 1) + fib(n - 2);
    memo.put(n, result);
    return result;
}
```

> Используйте мемоизацию для рекурсивных задач с перекрывающимися подзадачами (например, Фибоначчи, динамическое программирование).

> [!WARNING] Ловушка: рекурсия и StackOverflow
> Java **не оптимизирует хвостовую рекурсию**. Глубокая рекурсия (обход дерева, парсинг) вызывает `StackOverflowError`. Размер стека по умолчанию ~512KB-1MB. Замени рекурсию итерацией с явным стеком для глубоких структур.

- **Хвостовая рекурсия**: вызов метода — последняя операция. В Java не оптимизируется.
- **Итеративная альтернатива**: часто быстрее и экономичнее по памяти.
## Внутренняя реализация и производительность

- **while/for**: Компилируются в условные переходы и оптимизируются JIT-компилятором.
- **for-each**: Для массивов — индексный цикл, для коллекций — итератор.
- **stream().forEach()**: Использует Spliterator и лямбды, медленнее для простых операций, но удобен для сложных цепочек.
- **Рекурсия**: Каждый вызов — новая стековая рамка, без оптимизации хвостовой рекурсии.

> **Рекомендация:**
> - Используйте `for` для счётных циклов.
> - Используйте `while` для непредсказуемых итераций.
> - Используйте `for-each` или `stream().forEach()` для коллекций.
> - Заменяйте рекурсию итерацией для больших данных.
## Связанные темы

- [[Условные операторы]] — управляющие конструкции
- [[Java Stream API]] — функциональная итерация
- [[Интерфейсы Iterator и Iterable]] — итерация по коллекциям
- [[Массивы]] — перебор массивов

# Вопросы для собеседования по циклам в Java

1. Какие типы циклов поддерживает Java? В чём их различия?
2. Как работает цикл for? Какие компоненты он включает?
3. Чем отличается for от while? Когда использовать каждый?
4. Как работает do-while? В чём его особенность?
5. Что такое бесконечный цикл? Как его создать и остановить?
6. Как работает break и continue в циклах?
7. Что такое вложенные циклы? Как они работают?
8. Как использовать метки (labels) в циклах?
9. Как работает for-each цикл? Какие ограничения у него есть?
10. Как итерироваться по коллекциям в Java?
11. Как работает Stream API для итерации?
12. Что такое Iterator и Iterable? Как их использовать?
13. Как оптимизировать производительность циклов?
14. Как обрабатывать исключения в циклах?
15. Как работает параллельная обработка в циклах?
16. Что такое forEach в коллекциях?
17. Как использовать рекурсию вместо циклов?
18. Как работать с индексами в for-each цикле?
19. Как прервать внешний цикл из вложенного?
20. Как использовать Optional в циклах?
21. Как работать с примитивными массивами в циклах?
22. Как использовать лямбда-выражения в циклах?
23. Как обрабатывать null значения в циклах?
24. Как использовать yield в циклах (Java 14+)?
25. Какие проблемы могут возникнуть при работе с циклами в многопоточности?