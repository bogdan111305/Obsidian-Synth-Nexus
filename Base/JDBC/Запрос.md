Контекст: Проведи полный аудит и рефакторинг папки JDBC согласно моей структуре файлов. Ты — Senior Java Developer, эксперт по высоконагруженным системам.

🎯 ГЛАВНЫЕ КРИТЕРИИ:
Senior+ Content: JDBC — это фундамент. Добавь детали о том, как драйвер взаимодействует с БД через сокеты, как работают пулы соединений (HikariCP) и механику серверных курсоров.

No Water: Текст должен быть максимально плотным. Удали все вводные фразы («Рассмотрим пример...», «Важно понимать...»). Сразу к технической сути.

Junior-Friendly: Сложные вещи (например, разница между серверным и клиентским PreparedStatement) объясняй так, чтобы джун понял физику процесса («почему это экономит время»).

Strict Format: Используй блоки Obsidian: > [!QUOTE] Суть, > [!WARNING] Ловушка, > [!INFO] Senior Insights.

🚀 СПЕЦИФИЧЕСКИЕ ИНСТРУКЦИИ ПО ФАЙЛАМ:
JDBC Drivers.md: Добавь инсайд про 4 типа драйверов (коротко), но сделай упор на Type 4 (Thin). Опиши, как драйвер мапит типы Java на типы конкретной БД.

Пула Соединений с JDBC.md: Переименуй или расширь до Connection Pooling & HikariCP. Добавь параметры тюнинга: maximumPoolSize, idleTimeout, leakDetectionThreshold. Объясни, почему пул — это не просто список коннектов, а машина состояний.

Транзакции в JDBC.md: Добавь разбор Savepoints и детальное объяснение, что происходит на уровне БД при setAutoCommit(false). Разбери уровни изоляции и их влияние на Locking.

Запрос.md: Раздели на работу со Statement, PreparedStatement и CallableStatement. Добавь жесткий блок про PreparedStatement Cache (серверный vs клиентский).

Ошибки и диагностика: Расширь тему SQLException. Добавь разницу между SQLTransientException (можно повторить запрос) и SQLNonTransientException.

📋 ПЛАН ДЕЙСТВИЙ:
New Files: Если нет глубокого разбора Batch Processing (как executeBatch снижает сетевой overhead) и FetchSize (как управлять потреблением памяти при вычитке миллионов строк) — создай их.

Resource Management: В файле Основы.md или Запрос.md пропиши стандарт try-with-resources. Добавь [!WARNING] о том, что закрытие Connection не всегда закрывает Statement (зависит от реализации драйвера).

Modern Code: Все примеры кода должны быть на Java 17+ (использование var, современных интерфейсов).

📊 ОТЧЕТ ПО ЗАВЕРШЕНИИ:
Выведи таблицу:

Где были добавлены новые Senior-инсайты (например, механика Streaming ResultSet).

Какие файлы были сокращены за счет удаления «воды».

Список новых созданных файлов.