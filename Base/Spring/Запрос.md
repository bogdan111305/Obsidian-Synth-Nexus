Контекст: Проведи аудит, радикальное слияние и расширение папки Spring. Ты — Senior Java Architect. Твоя цель: превратить «файловую свалку» в системную базу знаний уровня Senior+, которая остается понятной для Junior-разработчика.

🎯 СТРАТЕГИЯ «СЛИЯНИЯ И ОЧИСТКИ» (Merge & Destroy):
Spring Data Cleanup (4.2): * Слей все (4+) файлы по PartTreeJpaQuery в один: "Query Methods & PartTree Logic".

Слей все (5+) файлы по RepositoryQuery и Proxy в один: "Spring Data Proxy & Query Execution".

Объедини EntityGraph и @EntityGraph в один файл.

Консолидация Core (Раздел 1): Слей все файлы по IoC и DI (включая Spring Core.md) в один фундаментальный гайд: "IoC Container & Dependency Injection".

Очистка: Удали все пустые или малоинформативные README.md. Ценную информацию из них перенеси в тематические файлы.

🚀 ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ (Senior+ Insights):
Даже если информации в папке недостаточно, ты обязан добавить глубокий разбор:

Core Internals: 3-уровневый кэш синглтонов для решения Circular Dependencies (singletonObjects, earlySingletonObjects, singletonFactories). Разница между JDK Dynamic Proxy и CGLIB.

Spring AOP: Почему @Transactional не работает при self-invocation. Механизм AnnotationAwareAspectJAutoProxyCreator.

Spring Data Magic: Как QueryExecutorMethodInterceptor перехватывает вызовы репозиториев. Работа DeferredRepositoryInitializationListener.

Spring Boot 3 & Modern Java: Механизм AutoConfiguration и Conditionals. Поддержка Virtual Threads (Project Loom) и адаптация под Java 21+.

Observability (Spring Side): Роль Spring Actuator и Micrometer как поставщиков метрик (без описания внешних систем типа Grafana).

🏗 ЗАПОЛНЕНИЕ ПУСТЫХ СТОЛПОВ (Missing Pillars):
Создай полноценные заметки (или скелеты, если данных совсем нет) для пустых разделов:

Spring Security (Раздел 3): Архитектура FilterChain, роль SecurityContextHolder, AuthenticationManager и Provider.

Testing Strategy: Разница между @SpringBootTest и Slice-тестами (@DataJpaTest, @WebMvcTest). Работа с @MockBean и @SpyBean.

Spring Web: Разбор DispatcherServlet, иерархия WebApplicationContext и реализация Scope (Request/Session) через ScopedProxy.

✍️ ФОРМАТ И СТИЛЬ:
Junior-Friendly: Используй емкие аналогии (например, "BPP — это тюнинг-ателье, которое дорабатывает уже созданный автомобиль-бин").

Senior-Focused: Плотный технический текст без воды в блоках:

> [!QUOTE] Суть — Лаконичное определение.

> [!WARNING] Ловушка — Ошибки на проде (например, прокси-через-self-invocation).

> [!INFO] Senior Insights — Внутрянка (Reflection, Bytecode, Spring SPI).

📊 ОТЧЕТ:
После завершения выдай таблицу:

Список поглощенных файлов (Deleted/Merged).

Список новых тем уровня Senior+, добавленных с нуля.

Топ-3 архитектурных инсайта, которые теперь есть в базе.