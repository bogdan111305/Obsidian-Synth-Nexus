Контекст: Создай в папке Git системную базу знаний. Ты — Senior DevOps Engineer и эксперт по системам контроля версий. Твоя цель: объяснить Git не как набор команд, а как консистентную структуру данных.

🏗 СТРУКТУРА ФАЙЛОВ (Иерархия):
Создай следующие файлы с глубоким наполнением:

Git Internals & Data Model.md: Как Git хранит данные. Разбор объектов: blob, tree, commit, tag. Понятие контентной адресации и SHA-1 хэшей.

Branching & Merging.md: Что такое ветка на самом деле (указатель). Разница между Merge (fast-forward vs 3-way) и Rebase. Когда что использовать.

The Three States & Staging.md: Глубокий разбор: Working Directory, Staging Area (Index), Repository. Как работает .git/index.

Advanced Commands & Rescue.md: Инструменты для исправления ошибок: git reflog (спасение удаленных веток), cherry-pick, stash, interactive rebase.

Git Workflows.md: Сравнение стратегий: Git Flow, GitHub Flow, Trunk-based development.

Configuration & Security.md: Настройка .gitignore, .gitattributes, работа с SSH/GPG ключами и хранение секретов (почему их нельзя пушить).

🚀 ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ (Senior+ Insights):
Git Plumbing vs Porcelain: Объясни разницу между высокоуровневыми и низкоуровневыми командами.

The Power of Reflog: Покажи, что в Git почти невозможно ничего удалить безвозвратно, если был создан коммит.

Detached HEAD: Объясни физику этого состояния и как из него выйти без потерь.

Hook System: Как автоматизировать проверки перед коммитом (pre-commit hooks).

✍️ ФОРМАТ И СТИЛЬ:
Junior-Friendly: Лаконичные аналогии. Например: "Коммит — это снимок всей файловой системы в конкретный момент, а не просто список изменений".

Senior-Focused: Технические детали в блоках:

> [!QUOTE] Суть — Архитектурное определение.

> [!WARNING] Ловушка — Опасные команды (например, force push и как он затирает историю коллег).

> [!INFO] Senior Insights — Внутрянка (как работает упаковка объектов в packfiles).

📊 ОТЧЕТ:
После генерации выведи таблицу:

Список созданных файлов.

Топ-3 "Mind-blowing" факта о внутренностях Git, которые теперь есть в базе.

Чек-лист: "Что должен знать каждый разработчик, чтобы не сломать репозиторий".